## 1. 分组迁移后端能力

- [x] 1.1 为账号管理新增分组迁移请求模型、路由和处理器入口，支持 `source_group_id`、`target_group_id`、`count` 等参数。
- [x] 1.2 在服务层实现分组迁移流程：查询源分组候选账号、校验同类型与可迁移资格、数量不足时整单失败、成功时完成目标分组绑定替换。
- [x] 1.3 补充分组迁移相关测试，覆盖成功迁移、数量不足失败、候选账号类型不一致失败、限流/占用账号被排除等场景。

## 2. 按分组范围执行批量操作

- [x] 2.1 为批量重置状态、批量刷新令牌和批量编辑请求增加可选 `scope_group_id` 语义，并在后端统一收敛“已选账号 ∩ 指定分组”的执行集合。
- [x] 2.2 保持批量编辑现有字段勾选语义不变，确保 `scope_group_id` 仅影响作用范围，不影响 `group_ids` 等实际写入字段语义。
- [x] 2.3 补充处理器和服务测试，覆盖按分组范围执行成功、范围内无账号失败，以及作用范围分组与目标写入分组同时存在的场景。

## 3. 管理后台交互与回归验证

- [x] 3.1 在账号列表批量操作区新增分组迁移入口，以及重置状态/刷新令牌的分组选择交互与提示文案。
- [x] 3.2 在批量编辑弹窗中新增作用范围分组选择，并确保未勾选字段继续保持原值。
- [x] 3.3 补充前端单元测试或交互测试，验证分组范围过滤、分组迁移表单校验和批量编辑勾选逻辑未回归。
