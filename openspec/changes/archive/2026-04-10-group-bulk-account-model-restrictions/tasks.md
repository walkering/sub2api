## 1. 分组侧账号批量更新接口

- [x] 1.1 为分组管理新增“按分组批量设置账号模型限制”接口，并由服务端解析目标分组下的账号集合。
- [x] 1.2 复用现有账号模型限制写入逻辑，将白名单 / 映射配置统一落到账号现有 `credentials.model_mapping` 结构。
- [x] 1.3 返回命中账号数、成功数和失败结果，并对空分组账号范围给出明确错误提示。

## 2. 管理后台分组管理入口

- [x] 2.1 在 `GroupsView` 中新增“批量设置账号模型限制”入口。
- [x] 2.2 复用或抽离现有账号批量编辑中的模型限制表单，使分组入口与账号入口行为一致。
- [x] 2.3 增加作用范围说明，明确修改的是账号本体配置，账号在其他分组中的使用也会受影响。

## 3. 测试与回归验证

- [x] 3.1 补充后端测试，覆盖分组账号范围解析、空分组处理和批量写入结果。
- [x] 3.2 补充前端测试，覆盖分组入口表单校验、提交行为和提示文案。
- [x] 3.3 回归验证账号页原有模型白名单 / 映射功能未受影响。
