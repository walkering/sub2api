## 1. CLI 参数与配置加载入口

- [ ] 1.1 在 `backend/cmd/server/main.go` 中新增 `-c` / `-config` 参数，并将其绑定到同一配置文件路径变量
- [ ] 1.2 在 `backend/internal/config` 中增加显式配置文件路径的统一设置/读取入口，让 `Load()` 与 `LoadForBootstrap()` 能优先读取指定文件
- [ ] 1.3 调整 `GetServerAddress()`，确保 setup 阶段的监听地址解析也遵循显式配置文件路径

## 2. 行为校验与错误处理

- [ ] 2.1 保持未传入 `-c` 时的默认搜索路径与环境变量覆盖行为不变
- [ ] 2.2 当显式配置文件不存在、不可读或格式错误时，返回明确错误并禁止静默回退到默认搜索路径
- [ ] 2.3 补充后端测试，覆盖短参数、长参数、setup/正式启动一致性、未传参回退，以及无效路径/非法 YAML 错误场景

## 3. 文档与运行示例

- [ ] 3.1 更新 README / 部署文档中的二进制运行示例，增加 `./sub2api -c config.yaml`
- [ ] 3.2 补充 CLI 帮助或相关说明，明确 `-c` / `-config` 的用途与优先级
