## 1. 设置接线

- [x] 1.1 为后端设置常量、`SystemSettings`、设置解析/保存逻辑和管理接口新增 `account_fair_rotation_enabled` 字段，并将默认值初始化为 `false`
- [x] 1.2 为前端系统设置类型、请求体和系统设置页新增账号公平轮转开关，并补充中英文文案

## 2. 调度策略实现

- [x] 2.1 梳理 `GatewayService`、`OpenAIGatewayService`、`GeminiMessagesCompatService` 的账号选择入口，抽取共享的公平轮转候选选择辅助逻辑
- [x] 2.2 在保持现有优先级、模型/平台过滤、额度限制、窗口成本和其他可调度性检查不变的前提下，实现“关闭走旧逻辑、开启走同优先级公平轮转”的选择分支
- [x] 2.3 为公平轮转增加按调度作用域维护的轮转游标或等效状态，并处理候选集变化、单候选账号和游标失效时的兜底行为

## 3. 粘性会话与兼容性

- [x] 3.1 确保公平轮转只作用于新的账号选择，已有粘性会话命中时继续复用原账号
- [x] 3.2 确保关闭开关时所有入口保持当前调度行为，不引入额外轮转副作用

## 4. 测试与验证

- [x] 4.1 增加后端测试，验证开关默认关闭、开启/关闭保存、关闭时兼容旧逻辑
- [x] 4.2 增加调度测试，验证开启后多个同优先级可调度账号会被连续新请求公平轮转、单候选账号仍可正常选中
- [x] 4.3 增加回归测试，验证高优先级账号不会因公平轮转被低优先级账号抢占，且粘性会话在开关开启时仍保持原账号
